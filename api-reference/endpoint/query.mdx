---
title: "Check Software EOL"
api: "POST https://eol.xeol.com/api/v1/query"
description: "This endpoint checks if the provided software identifier(s) are end-of-life"
---

### Body

<ParamField body="identifiers" type="object[]">
  list of software identifiers to check for end-of-life status

  <Expandable title="identifier">
    <ResponseField name="type" type="string">
      Software's identifier type.
      
      Can be either `cpe` `purl` `custom`
    </ResponseField>

    <ResponseField name="identifier" type="string">
      Unique identifier string for the software. If custom, use software name with vendor name
    </ResponseField>

    <ResponseField name="version" type="string">
      Software version
    </ResponseField>
  </Expandable>
</ParamField>

### Response

<ResponseField name="results" type="object[]">
  The contents of the user group.

  <Expandable title="result">
    <ResponseField name="identifier" type="string">
      Software identifier string for the matched artifact
    </ResponseField>

    <ResponseField name="version" type="string">
      Software version for the matched artifact
    </ResponseField>

    <ResponseField name="eol" type="boolean">
      Whether the matched artifact is end-of-life.
      
      `null` if an artifact could not be matched.
    </ResponseField>

    <ResponseField name="artifact" type="object">
      Matched artifact info. 
      
      `null` if an artifact could not be matched.

      <Expandable title="artifact">
        <ResponseField name="name" type="string">
          Name of the matched artifact
        </ResponseField>

        <ResponseField name="version" type="string">
          Release cycle of the matched artifact
        </ResponseField>

        <ResponseField name="type" type="string">
          Type of the matched artifact.
          
          Can be either `software` `package` `os` `product`
        </ResponseField>

        <ResponseField name="released" type="datetime">
          Release date of the matched artifact
        </ResponseField>

        <ResponseField name="lts" type="boolean">
          Whether the matched artifact has `Long Term Support`
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<RequestExample>
  ```bash Example Request
  curl --location --request POST 'https://eol.xeol.com/api/v1/query' \
  --header 'Content-Type: application/json' \
  --header 'Authorization: <XEOL_API_KEY>' \
  --data-raw '{
      "identifiers": [
        {
          "type": "cpe",
          "identifier": "cpe:/o:alpinelinux:alpine_linux",
          "version": "3.14"
        },
        {
          "type": "purl",
          "identifier": "pkg:python/python@3.8",
          "version": "3.8"
        }
      ]
  }'
  ```
</RequestExample>

<ResponseExample>
  ```json Response
  {
    "results": [
      {
        "identifier": "cpe:/o:alpinelinux:alpine_linux",
        "version": "3.14",
        "eol": true,
        "artifact": {
          "name": "Alpine",
          "version": "3.14",
          "type": "os",
          "released": "2021-06-15 00:00:00",
          "lts": false
        }
      },
      {
        "identifier": "pkg:python/python@3.8",
        "version": "3.8",
        "eol": false,
        "artifact": {
          "name": "Python",
          "version": "3.8",
          "type": "pkg",
          "released": "2019-10-14 00:00:00",
          "lts": false
        }
      }
    ]
  }
  ```
</ResponseExample>
